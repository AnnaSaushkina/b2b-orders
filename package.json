{
  "name": "loangate",
  "private": true,
  "version": "0.0.0",
  "description": "Кредитный конвейер — платформа управления ипотечными заявками",
  "scripts": {
    "dev": "concurrently -n client,server -c blue,green \"pnpm --filter client dev\" \"pnpm --filter server-nest start:dev\"",
    "dev:client": "pnpm --filter client dev",
    "dev:server": "pnpm --filter server-nest start:dev",
    "build": "pnpm --filter client build",
    "typecheck": "pnpm -r --if-present typecheck",
    "format": "prettier --write ."
  },
  "devDependencies": {
    "@types/node": "^26.1.1",
    "concurrently": "^10.0.3",
    "prettier": "^3.4.2"
  },
  "dependencies": {
    "@tanstack/react-query": "^5.101.2"
  }
}
